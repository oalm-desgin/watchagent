# Runtime dependencies only — kept lean so the production image stays small.
# Test/lint tooling lives in requirements-dev.txt.

fastapi>=0.115,<0.130
uvicorn[standard]>=0.34,<0.40
httpx>=0.27,<0.30
aiosqlite>=0.20,<0.22
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
structlog>=24.4,<26
